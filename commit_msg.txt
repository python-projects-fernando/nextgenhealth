feat(api): implement user search endpoint and restructure use cases

- Added API response DTOs (UserSummaryResponse) in src/api/responses/
- Implemented FindUserByEmailUseCase with FindUserByEmailQuery DTO
- Restructured use case modules into subdirectories (register_user, find_user_by_email) with command/use_case/query files
- Added infrastructure config (postgres_config.py) and dependency injection setup (postgres_dependencies.py) for database
- Updated UserCredentials with _from_hashed factory for persistence re-hydration
- Updated UserFactory to accept UserCredentials instance
- Refactored API dependencies and routes to use new structure and PostgreSQL repository
- Endpoint GET /users/{email} now returns public user data via UserSummaryResponse DTO
- Ensures credentials are not exposed in API responses